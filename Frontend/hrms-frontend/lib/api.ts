import axios from 'axios';import { useAuthStore } from './auth';export const api=axios.create({baseURL:process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api',timeout:8000});api.interceptors.request.use((config)=>{const token=useAuthStore.getState().token;if(token)config.headers.Authorization=`Bearer ${token}`;if(process.env.NODE_ENV==='development')console.info('[api]',config.method?.toUpperCase(),config.url);return config});api.interceptors.response.use((res)=>res,(error)=>{if(error?.response?.status===401&&typeof window!=='undefined'){useAuthStore.getState().logout();window.location.href='/auth/login'}return Promise.reject(error)});export async function withFallback<T>(request:Promise<{data:T}>,fallback:T):Promise<T>{try{return (await request).data}catch{return fallback}}
