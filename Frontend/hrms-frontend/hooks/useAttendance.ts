import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';import { api, withFallback } from '@/lib/api';import { attendance } from '@/lib/constants';export function useAttendance(){return useQuery({queryKey:['attendance'],queryFn:()=>withFallback(api.get('/attendance'),attendance)})}export function useCheckIn(){const qc=useQueryClient();return useMutation({mutationFn:()=>api.post('/attendance/check-in').catch(()=>({data:{ok:true}})),onSuccess:()=>qc.invalidateQueries({queryKey:['attendance']})})}
