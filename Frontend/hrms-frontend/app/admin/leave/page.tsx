'use client';import { useState } from 'react';import { PageTransition } from '@/components/shared/PageTransition';import { LeaveTable } from '@/components/leave/LeaveTable';import { Modal } from '@/components/shared/Modal';import { leaves } from '@/lib/constants';export default function AdminLeave(){const [open,setOpen]=useState(false);return <PageTransition><div className="flex items-center justify-between"><h1 className="gradient-text text-4xl font-black">Leave approvals</h1><button onClick={()=>setOpen(true)} className="gradient-button rounded-2xl px-5 py-3 font-bold">Open comment modal</button></div><div className="mt-6"><LeaveTable rows={leaves}/></div><Modal open={open} title="Approval comment" onClose={()=>setOpen(false)}><textarea className="input min-h-32" placeholder="Add HR comment..."/><button onClick={()=>setOpen(false)} className="gradient-button mt-4 rounded-2xl px-5 py-3 font-bold">Save decision</button></Modal></PageTransition>}
