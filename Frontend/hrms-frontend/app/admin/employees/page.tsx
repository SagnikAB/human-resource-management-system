'use client';import Link from 'next/link';import { PageTransition } from '@/components/shared/PageTransition';import { DataTable } from '@/components/shared/DataTable';import { StatusBadge } from '@/components/shared/StatusBadge';import { employees } from '@/lib/constants';export default function Employees(){return <PageTransition><h1 className="gradient-text text-4xl font-black">Employees</h1><div className="mt-6"><DataTable rows={employees as unknown as Record<string,unknown>[]} searchKey="name" columns={[{key:'employeeId',label:'ID'},{key:'name',label:'Name',render:(r)=><Link className="text-secondary" href={`/admin/employees/${r.id}`}>{String(r.name)}</Link>},{key:'email',label:'Email'},{key:'department',label:'Department'},{key:'salary',label:'Salary'},{key:'status',label:'Status',render:(r)=><StatusBadge status={String(r.status==='Active'?'Approved':r.status)}/>}]} /></div></PageTransition>}
