'use client';import { PageTransition } from '@/components/shared/PageTransition';import { PayslipCard } from '@/components/payroll/PayslipCard';import { SalaryBreakdown } from '@/components/payroll/SalaryBreakdown';import { usePayroll } from '@/hooks/usePayroll';export default function Payroll(){const {data=[]}=usePayroll();return <PageTransition><h1 className="gradient-text text-4xl font-black">Payroll</h1><div className="mt-6 grid gap-6 xl:grid-cols-[1fr_.8fr]"><div className="space-y-4">{data.map(p=><PayslipCard key={p.id} p={p}/>)}</div><SalaryBreakdown/></div></PageTransition>}
