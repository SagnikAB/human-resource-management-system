'use client';import { motion } from 'framer-motion';import { CalendarCheck, Clock, DollarSign, Plane } from 'lucide-react';import { PageTransition, stagger } from '@/components/shared/PageTransition';import { StatCard } from '@/components/dashboard/StatCard';import { QuickActions } from '@/components/dashboard/QuickActions';import { ActivityFeed } from '@/components/dashboard/ActivityFeed';import { format } from 'date-fns';export default function Dashboard(){return <PageTransition><div className="mb-6"><p className="text-muted">{format(new Date(),'p')} local time</p><h1 className="gradient-text text-4xl font-black">Employee cockpit</h1></div><motion.div variants={stagger} initial="hidden" animate="show" className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard title="Today's Attendance" value={1} suffix=" checked" icon={<CalendarCheck/>}/><StatCard title="Leave Balance" value={18} suffix=" days" icon={<Plane/>}/><StatCard title="Pending Requests" value={2} icon={<Clock/>}/><StatCard title="Salary" value={6860} prefix="$" icon={<DollarSign/>}/></motion.div><div className="mt-6"><QuickActions/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><div className="glass rounded-3xl p-6"><h2 className="text-xl font-bold">Attendance streak</h2><p className="mt-2 text-muted">You are on a 14-day perfect check-in streak. Keep the pulse alive.</p><div className="mt-6 flex gap-2">{Array.from({length:14}).map((_,i)=><motion.span key={i} initial={{height:8}} animate={{height:28+i%5*6}} transition={{delay:i*.04}} className="w-full rounded-full bg-gradient-to-t from-primary to-secondary"/>)}</div></div><ActivityFeed/></div></PageTransition>}
