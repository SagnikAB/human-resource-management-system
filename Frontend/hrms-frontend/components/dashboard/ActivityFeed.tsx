'use client';import { motion } from 'framer-motion';import { activities } from '@/lib/constants';export function ActivityFeed(){return <div className="glass rounded-3xl p-5"><h2 className="mb-4 text-lg font-bold">Recent activity</h2><div className="space-y-3">{activities.map((a,i)=><motion.div key={a} initial={{opacity:0,x:-12}} animate={{opacity:1,x:0}} transition={{delay:i*.08}} className="flex items-center gap-3 rounded-2xl bg-white/[.03] p-3"><span className="h-2 w-2 rounded-full bg-secondary"/><span className="text-sm text-slate-300">{a}</span></motion.div>)}</div></div>}
