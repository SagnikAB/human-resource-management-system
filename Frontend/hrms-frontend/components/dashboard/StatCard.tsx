'use client';import { motion } from 'framer-motion';import { AnimatedNumber } from '@/components/shared/AnimatedNumber';import { item } from '@/components/shared/PageTransition';export function StatCard({title,value,prefix='',suffix='',icon}:{title:string;value:number;prefix?:string;suffix?:string;icon:React.ReactNode}){return <motion.div variants={item} whileHover={{y:-4,scale:1.01}} className="glass rounded-3xl p-5"><div className="mb-5 flex items-center justify-between"><p className="text-sm text-muted">{title}</p><div className="rounded-2xl bg-primary/15 p-3 text-primary">{icon}</div></div><p className="text-3xl font-black"><AnimatedNumber value={value} prefix={prefix} suffix={suffix}/></p></motion.div>}
