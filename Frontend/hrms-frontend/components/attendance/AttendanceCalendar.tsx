import { AttendanceRecord } from '@/types';import { StatusBadge } from '@/components/shared/StatusBadge';const color:{[k:string]:string}={Present:'bg-success',Absent:'bg-danger','Half-day':'bg-warning',Leave:'bg-secondary'};export function AttendanceCalendar({records}:{records:AttendanceRecord[]}){return <div className="glass rounded-3xl p-5"><h2 className="mb-4 text-lg font-bold">July 2026 calendar</h2><div className="grid grid-cols-7 gap-2 text-center text-xs text-muted">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d=><div key={d}>{d}</div>)}{Array.from({length:3}).map((_,i)=><div key={'b'+i}/>)}{Array.from({length:31},(_,i)=>{const r=records.find(x=>Number(x.date.slice(-2))===i+1);return <div key={i} className="min-h-20 rounded-2xl border border-border bg-white/[.02] p-2 text-left"><p className="font-semibold text-slate-300">{i+1}</p>{r&&<span className={`mt-3 block h-2 w-2 rounded-full ${color[r.status]}`}/>}</div>})}</div><div className="mt-4 flex flex-wrap gap-2">{['Present','Absent','Half-day','Leave'].map(s=><StatusBadge key={s} status={s}/>)}</div></div>}
