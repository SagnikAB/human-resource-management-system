'use client';import { LeaveRequest } from '@/types';import { DataTable } from '@/components/shared/DataTable';import { StatusBadge } from '@/components/shared/StatusBadge';export function LeaveTable({rows}:{rows:LeaveRequest[]}){return <DataTable rows={rows as unknown as Record<string,unknown>[]} searchKey="employeeName" columns={[{key:'id',label:'ID'},{key:'employeeName',label:'Employee'},{key:'type',label:'Type'},{key:'from',label:'From'},{key:'to',label:'To'},{key:'days',label:'Days'},{key:'status',label:'Status',render:(r)=><StatusBadge status={String(r.status)}/>}]} />}
