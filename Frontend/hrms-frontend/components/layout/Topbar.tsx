'use client';import { format } from 'date-fns';import { Bell, Search } from 'lucide-react';import { useAuthStore } from '@/lib/auth';export function Topbar(){const user=useAuthStore(s=>s.user);return <header className="sticky top-0 z-30 flex items-center justify-between border-b border-border bg-background/70 px-4 py-4 backdrop-blur-xl md:px-8"><div><p className="text-sm text-muted">{format(new Date(),'EEEE, MMMM d, yyyy')}</p><h1 className="text-xl font-bold">Welcome back, {user?.name?.split(' ')[0]||'Champion'}</h1></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-2xl border border-border bg-surface px-3 py-2 md:flex"><Search className="h-4 w-4 text-muted"/><span className="text-sm text-muted">Search HRMS</span></div><button className="rounded-2xl border border-border bg-surface p-3"><Bell className="h-4 w-4"/></button><div className="grid h-10 w-10 place-items-center rounded-2xl gradient-button font-bold">{(user?.name||'U')[0]}</div></div></header>}
